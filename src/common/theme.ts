import styled, { DefaultTheme, createGlobalStyle } from 'styled-components';

const readerTheme: DefaultTheme = {
  colors: {
    main: 'cyan',
    secondary: 'magenta',
  },
};

export { readerTheme };
